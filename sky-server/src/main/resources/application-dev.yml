sky:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    host: localhost
    port: 3306
    database: sky_take_out
    username: root
    password: root
  aliyun:
    endpoint: ${alioos.endpoint}
    access-key-id: ${alioos.access-key-id}
    access-key-secret: ${alioos.access-key-secret}
    bucket-name: ${alioos.bucket-name}